# WordPress core, WooCommerce and other third-party plugins/themes, uploads,
# and local config — installed via wp-cli, not versioned. Only theme/ and
# plugin/ (this repo's actual source) are tracked.
wordpress/

.DS_Store
*.log

# Stray local artifacts, not part of the app
db.sqlite3
*.docx
